<UserControl
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d"
    x:Class="FireDetectionSystem.Views.ImageDetectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FireDetectionSystem.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock
                FontSize="20"
                Foreground="White"
                Margin="10,3,0,3"
                Text="置信度:" />
            <Slider
                IsSnapToTickEnabled="True"
                Margin="10,10,3,3"
                Maximum="1"
                Minimum="0"
                TickFrequency="0.1"
                Value="{Binding Confidence, Mode=TwoWay}"
                Width="200" />

            <Label
                Content="{Binding Confidence}"
                FontSize="20"
                Foreground="White" />


            <Button
                Command="{Binding SelectImagePathCommand}"
                Content="选择图片"
                HorizontalAlignment="Right" />
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Border
                BorderBrush="Black"
                BorderThickness="2"
                Margin="5"
                Padding="10">
                <Image
                    Source="{Binding ImagePath}"
                    Stretch="Fill"
                    x:Name="image1" />
            </Border>
            <Border
                BorderBrush="Black"
                BorderThickness="2"
                Grid.Column="1"
                Margin="5"
                Padding="10">
                <Image
                    Source="{Binding DetectImage}"
                    Stretch="Fill"
                    x:Name="image2" />
            </Border>
        </Grid>
        <Grid Grid.Row="2">
            <StackPanel Orientation="Horizontal">
                <ListView ItemsSource="{Binding Items1}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn DisplayMemberBinding="{Binding Code}" Header="Code" />
                            <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name" />
                            <GridViewColumn DisplayMemberBinding="{Binding Description}" Header="Description" />
                            <GridViewColumn Header="Options">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox Width="100">
                                            <ComboBoxItem Content="Test" />
                                            <ComboBoxItem Content="Test2" />
                                            <ComboBoxItem Content="Test3" />
                                        </ComboBox>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
                <Button
                    Command="{Binding DetectionImageCommand}"
                    Content="开始检测"
                    HorizontalAlignment="Right"
                    Margin="0,5,5,5"
                    Width="100" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
