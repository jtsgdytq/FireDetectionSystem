<UserControl x:Class="FireDetectionSystem.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:FireDetectionSystem.Views"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MaterialDesignPaper}">

    <UserControl.Resources>
        <local:RoleToColorConverter x:Key="RoleToColorConverter"/>
        <local:StatusToColorConverter x:Key="StatusToColorConverter"/>
        <local:StatusToTextConverter x:Key="StatusToTextConverter"/>
        <local:StatusToIconConverter x:Key="StatusToIconConverter"/>
        <local:StatusToToggleTooltipConverter x:Key="StatusToToggleTooltipConverter"/>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="用户管理"
                       Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                       VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding RefreshCommand}"
                        Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Refresh" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBlock Text="刷新"/>
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{Binding AddUserCommand}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <TextBlock Text="添加用户"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- 搜索和筛选栏 -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 搜索框 -->
            <TextBox Grid.Column="0"
                     materialDesign:HintAssist.Hint="搜索用户名、姓名或邮箱"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,8,0">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding RefreshCommand}"/>
                </TextBox.InputBindings>
            </TextBox>

            <!-- 角色筛选 -->
            <ComboBox Grid.Column="1"
                      materialDesign:HintAssist.Hint="角色"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      ItemsSource="{Binding RoleFilters}"
                      SelectedItem="{Binding SelectedRoleFilter}"
                      Margin="0,0,8,0"/>

            <!-- 状态筛选 -->
            <ComboBox Grid.Column="2"
                      materialDesign:HintAssist.Hint="状态"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}"
                      ItemsSource="{Binding StatusFilters}"
                      SelectedItem="{Binding SelectedStatusFilter}"/>
        </Grid>

        <!-- 用户列表 -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding FilteredUsers}"
                  SelectedItem="{Binding SelectedUser}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  materialDesign:DataGridAssist.CellPadding="8"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="8">
            <DataGrid.Columns>
                <!-- ID -->
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>

                <!-- 用户名 -->
                <DataGridTextColumn Header="用户名" Binding="{Binding Username}" Width="120"/>

                <!-- 姓名 -->
                <DataGridTextColumn Header="姓名" Binding="{Binding FullName}" Width="120"/>

                <!-- 邮箱 -->
                <DataGridTextColumn Header="邮箱" Binding="{Binding Email}" Width="180"/>

                <!-- 角色 -->
                <DataGridTemplateColumn Header="角色" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding Role, Converter={StaticResource RoleToColorConverter}}"
                                    CornerRadius="4" Padding="8,4">
                                <TextBlock Text="{Binding Role}"
                                           Foreground="White"
                                           HorizontalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- 状态 -->
                <DataGridTemplateColumn Header="状态" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding IsActive, Converter={StaticResource StatusToColorConverter}}"
                                    CornerRadius="4" Padding="8,4">
                                <TextBlock Text="{Binding IsActive, Converter={StaticResource StatusToTextConverter}}"
                                           Foreground="White"
                                           HorizontalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- 最后登录 -->
                <DataGridTextColumn Header="最后登录" Binding="{Binding LastLoginAt, StringFormat={}{0:yyyy-MM-dd HH:mm}}" Width="140"/>

                <!-- 操作 -->
                <DataGridTemplateColumn Header="操作" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <!-- 编辑按钮 -->
                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="编辑"
                                        Command="{Binding DataContext.EditUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="0,0,4,0">
                                    <materialDesign:PackIcon Kind="Pencil"/>
                                </Button>

                                <!-- 启用/禁用按钮 -->
                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="{Binding IsActive, Converter={StaticResource StatusToToggleTooltipConverter}}"
                                        Command="{Binding DataContext.ToggleUserStatusCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="0,0,4,0">
                                    <materialDesign:PackIcon Kind="{Binding IsActive, Converter={StaticResource StatusToIconConverter}}"/>
                                </Button>

                                <!-- 重置密码按钮 -->
                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="重置密码"
                                        Command="{Binding DataContext.ResetPasswordCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="0,0,4,0">
                                    <materialDesign:PackIcon Kind="Key"/>
                                </Button>

                                <!-- 删除按钮 -->
                                <Button Style="{StaticResource MaterialDesignIconButton}"
                                        ToolTip="删除"
                                        Command="{Binding DataContext.DeleteUserCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Foreground="Red">
                                    <materialDesign:PackIcon Kind="Delete"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 加载指示器 -->
        <Grid Grid.Row="2" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                         Value="0"
                         IsIndeterminate="True"
                         Width="50" Height="50"/>
        </Grid>
    </Grid>
</UserControl>
